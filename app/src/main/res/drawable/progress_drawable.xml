<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dip" />
            <gradient
                android:startColor="?attr/colorTagline"
                android:centerColor="?attr/colorTagline"
                android:centerY="0.75"
                android:endColor="?attr/colorTagline"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2dip" />
                <gradient
                    android:startColor="#80ffd300"
                    android:centerColor="#80ffb600"
                    android:centerY="0.75"
                    android:endColor="#a0ffcb00"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="10dip" />
                <gradient
                    android:startColor="?attr/colorReadingBar"
                    android:centerColor="?attr/colorReadingBar"
                    android:centerY="0.75"
                    android:endColor="?attr/colorReadingBar"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>